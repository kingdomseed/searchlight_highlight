## 0.1.0

- Initial strict-parity Dart reimplementation of Orama Highlight.
- Adds the `Highlight` class, `highlightStrategy`, `HighlightOptions`, and
  `Position`.
- Matches Orama-style HTML rendering, inclusive positions, and `trim()`
  behavior.
